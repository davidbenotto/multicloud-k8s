{
  "name": "clusters-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "db:start": "docker start clusters-db 2>/dev/null || docker compose -f docker/docker-compose.yml up -d postgres",
    "db:stop": "docker stop clusters-db",
    "predev": "npm run db:start",
    "dev": "concurrently \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
